<%= form_for(@turd) do |f| %>
  <% if @turd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@turd.errors.count, "error") %> prohibited this turd from being saved:</h2>

      <ul>
      <% @turd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
